const view = require('./View');
const viewTypes = require('./View.types');
const viewStyles = require('./View.styles');
const viewView = require('./View.view');

module.exports = [view, viewTypes, viewStyles, viewView];
